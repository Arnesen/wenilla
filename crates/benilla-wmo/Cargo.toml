[package]
name = "benilla-wmo"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "WMO (root + group) reader for WoW 1.12.1 (build 5875) — in-repo, decision 0021"

[dependencies]
benilla-bytes = { path = "../benilla-bytes" }
